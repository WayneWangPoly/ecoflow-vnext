# Active-order key cache operational note

Cache refresh warnings do not invalidate mirrored commercial data. Resolve them independently while using mirror health as the source of truth for completeness.
