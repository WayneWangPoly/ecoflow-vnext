Validation focus: Warehouse Map multi-SKU slots render immediately; fixed shelf mappings feed Pick; role-specific operation labels replace generic packaging operations; Ordermentum Inbox remains explicit; Store price matrix survives a broken aggregate view; Inventory exposes Warehouse Map and Quick Pick shortcuts; order values derive from line totals when invoice totals are absent.
