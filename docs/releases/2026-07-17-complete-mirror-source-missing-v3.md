# Complete mirror source-missing v3

The complete-mirror verifier now distinguishes retained source history from live EcoFlow fulfilment. `SOURCE_MISSING` orders remain non-blocking history unless a non-terminal internal EcoFlow order still exists. Settings also displays the persisted blocker list when history is already complete.
