# Warehouse Map multi-SKU release

Production behaviour:

- Select the A or B physical cell before pressing `+`.
- `+` adds one more configured SKU position to that same cell; it does not change stock quantity.
- The selected location is carried into Barcode setup and controlled Receive.
- Owner/Admin Edit layout can change the rack display name and category description while the stable rack and location codes remain unchanged.
