# EcoFlow Ordermentum Internalisation + Barcode Gate v3

Fixes v2 migration errors caused by aggregating uuid values with `max()` and keeps the barcode workbench alias stable.

Run `supabase/migrations/20260702_ordermentum_internalisation_barcode_gate_v3.sql` in Supabase SQL Editor.
