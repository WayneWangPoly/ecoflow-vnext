name: Commercial Promotion Wave 2 P4D promotion readiness

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/commercial-promotion-wave2-p4d-promotion-readiness-check.yml'
      - 'docs/engineering/work-packages/338-COMMERCIAL-WAVE2-P4D-PROMOTION-AUTHORITY-PLAN.md'
      - 'scripts/commercial-promotion-wave2-p4d-promotion-readiness-contract.test.mjs'
      - 'scripts/commercial-promotion-wave2-p4d-promotion-readiness-db-contract-test.sql'
      - 'src/data/repositories/commercialWave2P4DPromotionReadiness.ts'
      - 'src/features/productIdentity/CommercialWave2P4DPromotionReadinessCarrier.tsx'
      - 'src/features/productIdentity/commercialWave2P4DPromotionReadinessContract.ts'
      - 'src/features/productIdentity/ProductIdentityCommissioningWithSurvey.tsx'
      - 'supabase/migrations/20260914023000_commercial_wave2_p4d_promotion_authority_plan.sql'
  push:
    branches:
      - 'agent/product-identity/338-wave2-p4d-promotion-authority-plan'

permissions:
  contents: read

jobs:
  contract:
    name: Exact-head P4D promotion authority + plan contract
    runs-on: ubuntu-latest
    timeout-minutes: 45
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      PGHOST: localhost
      PGPORT: 5432
      PGUSER: postgres
      PGPASSWORD: postgres
      PGDATABASE: postgres
      EXACT_BASE_SHA: d5ad5178ba41149a12b4c308b0b4aabf1bf2a057
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Bind exact branch head to P4C production base
        shell: bash
        run: |
          set -euo pipefail
          test "$(git merge-base HEAD "$EXACT_BASE_SHA")" = "$EXACT_BASE_SHA"
          echo "exact_head=$(git rev-parse HEAD)"
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Exercise P4D static promotion authority contract
        run: node --test scripts/commercial-promotion-wave2-p4d-promotion-readiness-contract.test.mjs
      - name: Exercise PostgreSQL 17 P4D readiness and dormant promotion contract
        run: psql -v ON_ERROR_STOP=1 -f scripts/commercial-promotion-wave2-p4d-promotion-readiness-db-contract-test.sql
      - name: Keep P4C activation contract green
        run: node --test scripts/commercial-promotion-wave2-p4c-activation-contract.test.mjs
      - name: Keep P4B authority replacement contract green
        run: node --test scripts/commercial-promotion-wave2-p4b-authority-replacement-contract.test.mjs
      - name: Keep P2B promotion contract green
        run: node --test scripts/commercial-promotion-wave2-p2b-canary-promotion-contract.test.mjs
      - name: Typecheck
        run: npm run typecheck
      - name: Production build
        run: npm run build
      - name: Reject whitespace errors
        run: git diff --check "$EXACT_BASE_SHA"...HEAD
